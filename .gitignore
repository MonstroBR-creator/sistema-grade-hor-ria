node_modules/
*.log
.DS_Store
.env

# Banco de dados local (gerado por `npm run importar`)
backend/database/*.db
backend/database/*.db-journal
backend/database/*.db-wal
backend/database/*.db-shm

# Artefatos Python
__pycache__/
*.pyc

# Backups gerados por `npm run backup` (contem CPF e hashes de senha)
backups/

# Credenciais locais - NUNCA versionar
neon_url.txt
*_url.txt
*_api_key.txt
.env
